# DST Mods Monorepo

该仓库用于存放多个 DST Mod，并按类型分类。

## 目录约定

- `Build/`：建筑类 Mod
- 后续可扩展：`Combat/`、`Utility/`、`UI/` 等

## 当前 Mod

- `Build/luxury_courtyard/`：豪华庭院（室内建筑原型 Mod）

## 本地测试方式

将某个具体 Mod 目录（例如 `Build/luxury_courtyard/`）整体复制到 DST 的 `mods` 目录进行测试。
